#pragma once

// Thin route alias kept so callers can include a format-specific entrypoint
// while the real implementation lives in the raw transpose converter.
#include "../compressed_transpose_raw.cuh"
