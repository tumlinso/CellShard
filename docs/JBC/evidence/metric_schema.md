# JBC validation metric schema

Every accepted record names one biological mechanism and a global fixture identity. Complete cost includes discovery, build, persistence, reload, staging, execution, validation, mutation, and collection. Records report integer nanoseconds, bytes moved, useful interactions, launches, exact-output status, and whether the input is a matched biological null. A partial phase mask or inexact output is not promotion evidence.
